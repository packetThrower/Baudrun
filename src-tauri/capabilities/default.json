{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main window plus any tear-off windows spawned via `open_profile_window` (labels are `win-<uuid>`).",
  "windows": ["main", "win-*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:default",
    "opener:default",
    "log:default",
    "updater:default",
    "process:default"
  ]
}
