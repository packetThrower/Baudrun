{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set: main window, the dedicated Settings window (label `settings`), and any tear-off profile windows spawned via `open_profile_window` (labels `win-<uuid>`).",
  "windows": ["main", "settings", "win-*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:default",
    "opener:default",
    "log:default",
    "updater:default",
    "process:default"
  ]
}
